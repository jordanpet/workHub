{
  "name": "hub-executor-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["hub-executor"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/hub-executor-e2e/jest.config.ts",
        "passWithNoTests": true
      },
      "dependsOn": ["hub-executor:build", "hub-executor:serve"]
    }
  }
}
